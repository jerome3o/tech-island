apiVersion: v1
kind: Service
metadata:
  name: app-template
  labels:
    app: app-template
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-template
