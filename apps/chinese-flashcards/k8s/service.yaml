apiVersion: v1
kind: Service
metadata:
  name: chinese-flashcards
  labels:
    app: chinese-flashcards
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: chinese-flashcards
